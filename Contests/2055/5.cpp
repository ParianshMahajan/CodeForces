#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ll t;
    cin >> t;
    
    while (t--) {
        ll n;
        cin >> n;

        vector<pair<int,int>> a(n);
        ll ans=0;
        for (int i = 0; i < n; i++) {
            cin >> b[i];
            diff[i] = a[i] - b[i];

        }
    }

    return 0;
}
